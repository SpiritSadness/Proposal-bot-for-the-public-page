# Security

If you clone this repo for local use:

1. Copy `.env.example` → `.env`
2. Put a **new** bot token from @BotFather (do not reuse any token that was ever committed publicly)
3. Never commit `.env`
